# BACKLOG

Status: Living document. Always reflects current state.

---

## Now
- [ ] **User**: delete the `Potatoe415/coinchapp` GitHub repo (revised 2026-09-17 from the earlier "archive" decision — user now wants it deleted, not archived). Agent has no `gh` CLI and deliberately did not extract the stored git credential to do this via API (same secret-handling caution as the Vercel token). Manual: github.com/Potatoe415/coinchapp/settings → Danger Zone → Delete this repository. `Potatoe415/tranquil` stays untouched until its own repoint (below) is done.
- [ ] **User, optional**: the test game created during `coinchapp` verification (room code `JQW`) self-expires via the existing 48h TTL cron (`cleanup-expired-games`) — no action needed. To remove it immediately instead, run `delete from public.games where room_code = 'JQW';` in the Supabase SQL editor (agent cannot: `games`/`game_players` have RLS with no policies, only `service_role` can write, and the agent was never given that key).
- [ ] Do the same Vercel repoint for `tranquil` (`prj_FnorlK5RzjApMxvvVAXIhEvlxBdF`) — not started yet. Same approach as `coinchapp` (see `docs/DECISIONS.md` 2026-09-17 for the full playbook): new project linked to `Potatoe415/muchogames` with Root Directory `apps/tranquil`, copy non-sensitive env vars via API, ask the user to manually re-enter any "sensitive"-type ones, redeploy, verify live (including any Supabase-backed flow), then move `tranquil-woad.vercel.app` over.
- [ ] Fix `apps/coinchapp/lib/client/useMatchStats.ts:220` — `react-hooks/set-state-in-effect` lint **error** (`setAccum` called synchronously inside an effect body), surfaced by the new `npm run lint:all` (Turborepo) since root `npm run lint` excludes `apps/**`. Pre-existing in `coinchapp`'s own code, not touched — flag only, per `AGENTS.md`.
- [ ] Decide whether/how to bring `apps/tranquil` into the Turborepo/workspace graph. Blocked today by its own nested npm workspaces (`shared`+`client`), which cannot nest inside this repo's root workspace. Options: flatten `tranquil`'s internal layout (its own decision to make), or leave it permanently self-managed (`cd apps/tranquil && npm run build:shared && npm run build -w client`) since Turborepo caching matters most for `coinchapp`'s heavier Next.js build anyway.
- [ ] Confirm `/admin` renders and logs in from a real browser. The 2026-08-30 fix was verified at HTTP level only (every resource 200 with the right MIME, CSP checked against the full resource list) because the browser MCP would not register, so the render and the login round-trip are unconfirmed in a browser.
- [ ] Delete the verification row from the live test: `delete from public.muchogames_events where game_id = '__verification__';`. Harmless, but it currently sits at the top of the `/admin` ranking.
- [ ] Rotate `ADMIN_PASSWORD` — the value set on 2026-08-30 is a short demo password used to test the flow. It doubles as the HMAC key signing session tokens, so a weak value weakens the tokens as well, and the throttle does not compensate: it is in-memory, so it is per warm serverless instance and resets on cold start. Use a long random value: `vercel env add ADMIN_PASSWORD production --sensitive --force`, then redeploy.
- [ ] Copy `SUPABASE_URL` to the Preview environment. It is Production-only while `SUPABASE_SERVICE_ROLE_KEY` covers Preview and Production, and `api/_lib/supabase.js` throws unless both are present — so every database-backed route returns 500 on Preview deployments. Both values are stored Sensitive and cannot be read back, so only the user can copy it.
- [ ] Verify Yatzy online play with two real devices (create, join, leave, resume, reclaim a seat by code). The API-level guard is already verified live; the browser flow is not.
- [ ] Decide on the GitHub branch-protection rule: the 2026-08-30 push reported "Bypassed rule violations — Changes must be made through a pull request". Either adopt PRs or remove the rule.
- [ ] Make the "admin not configured" case diagnosable: `api/admin/login.js` sends it as `server-error`, so the 5xx redaction hides the reason from the operator. A 4xx or a dedicated `not-configured` code would surface it without leaking anything.

## Next
Audit action plan, in order (audit run 2026-08-30, priorities 1 to 5; priorities 1 to 4 done, priority 5 dead-weight deletion done 2026-09-09):
- [ ] Ship a genuinely lighter mobile banner. `public/assets/banner-games-hub-mobile.jpg` is byte-identical to `banner-games-hub.jpg` (same SHA-256, 554,461 bytes), so the `srcset` in `index.html` is a placebo — phones download the full-size image. Needs a real resized asset; generating one would mean adding an image dependency such as `sharp`, which is the user's call.
- [ ] Clear the 13 remaining lint warnings, then add `--max-warnings 0` so drift cannot accumulate silently. Mostly dead code (`detectInitialLocale`, `calculateMinMaxDelta`, `handleLanguageSelection`, `isStraight`, `vegLabel`) and 1 unused `catch (error)` binding in `public/games/yatsy/app.js` that should be a bare `catch {}`. Left untouched on purpose: `AGENTS.md` says flag pre-existing dead code, do not delete it.
- [ ] Decide whether to add automated tests for `scoring.js` (Yatzy) and `shared/js/engine.js`.
- [ ] Decide whether to formalize a shared error-handling/logging convention (see `docs/TECH.md` Open_Questions).

## Later
- [ ] Continue splitting `public/games/yatsy/app.js` (1187 lines after extracting `storage.js`, `render.js` and `session.js` on 2026-09-09; was 2204 right before this pass, ~1759 when last measured on 2026-08-30 before that — it keeps growing faster than it shrinks since it is under active development). What is left is the core game engine: dice/turn/scoring state, robot AI, defeat-mode auto-play, and the DOM-flight scoring animation shared by both local and remote scoring. It is one cohesive concern (all of it reads/mutates the same `state` object every turn), so the 300-line limit will likely need a different cut than the render/session split (e.g. by feature — robot AI, defeat mode — rather than by layer). `public/games/millionaire/app.js` (1069), `public/games/pyramide/app.js` (847), `wordplayer.js` (700) are still over the 300-line project limit too. Follow the split already used in `olemains`.
- [ ] Serve `@supabase/supabase-js` from the npm dependency instead of the unpinned runtime CDN import in `public/games/yatsy/matchmaking.js` (`https://esm.sh/...`, no SRI).
- [ ] Accessibility pass on the hub: `role="tablist"`/`aria-selected` on category tabs, keyboard support and `aria-expanded` on the language switcher (its closed options are invisible but still tab-focusable), `:focus-visible` styles, `prefers-reduced-motion`, and update `<html lang>` when the language changes.
- [ ] Deduplicate the rules-modal loading logic repeated across several games (`docs/TECH.md` Open_Questions).
- [ ] Author a `NOTES.md` (per `docs/GAMES_MAP.md`'s convention) for each already-oversized game — `millionaire`, `pyramide`, `yatsy` — so future edits can skip reading the full source. Skipped on 2026-09-17 since the request was forward-looking (new games), not retroactive.
- [ ] Fix `public/games/pictionary/index.html` + `main.js`: orphaned, not referenced by `hub-config.json` (Pictionary actually launches via `wordplayer.html?game=pictionary`). Flagged while building `docs/GAMES_MAP.md` on 2026-09-17, left untouched per `AGENTS.md` (pre-existing dead code, do not delete without being asked).
- [ ] Extend the admin page further if wanted: hub visits, per-language split, live event feed. The daily trend and the range switch are done (2026-08-30). Anything visitor-shaped — unique visitors, sessions, live presence, devices, countries, referrers — needs new columns on `muchogames_events`, a change to `api/track.js`, an update to `docs/DATA_MODEL.md`, and a deliberate choice to collect much more personal data; declined once on 2026-08-30.
- [ ] Split `public/admin/admin.css` when it next grows: it is at 271 effective lines against a 300-line limit, and it now mixes login, dashboard, ranking and chart styles.
- [ ] Finish the "bergamots" to "muchogames" rename: Vercel project renamed (user, 2026-09-17), GitHub repo `Potatoe415/bergamots` → `Potatoe415/muchogames` renamed (user, 2026-09-17, local `origin` remote updated to match) and `package.json`/README/titles/manifest/hardcoded hub URLs updated (agent, same day). Still not renamed: `localStorage` keys (`bergamots-lang`, `bergamots-player-name`, `bergamots-player-avatar*`, `bergamots-auth`, `bergamots-launch-counts`) and the `yatzy_*` Supabase tables — deliberately left alone since renaming either resets/migrates real user data, a separate decision from the cosmetic rename.

## Blocked
- [ ] TBD

  ## Done
- [x] 2026-09-17 — Fixed a real production Yatzy layout bug reported from a Motorola Edge 50 Fusion (screenshot: "Quitter" button hidden behind the score chip, scoreboard columns overlapping FULL/CARRE/SUITE/MIN/MAX/YATZY). Root cause: `.score-group`'s grid columns had a hard `rem` floor (`minmax(5.25rem, 1.08fr) minmax(4rem, 0.92fr) minmax(4rem, 0.92fr)`, ~424px minimum for the two groups combined) and `h1` had a `clamp()` floor of `2rem` — both only safe below the existing `max-width: 430px` breakpoint, leaving a "dead zone" on wider-but-still-phone-sized viewports (common on large low-DPR Android screens) where content could outgrow its box and visually overlap a sibling (`.score-card`, having no `min-width` override, overflowed left over the "Quitter" button once `.header-title` ran out of room). Fixed by making the base rules fluid instead of breakpoint-dependent: `.score-group` now uses pure `fr` columns (no rem floor) + `min-width: 0`; `h1` floor lowered to `1.5rem` plus real `text-overflow: ellipsis` truncation; `.header-title` can now shrink (`flex: 0 1 auto; min-width: 0`) while `.restart-button` got `flex-shrink: 0` so it's never the one that disappears. Removed the now-redundant duplicate rule this created in the `max-width: 430px` block. Also bumped `public/games/yatsy/sw.js` `CACHE_NAME` `v33` → `v34`: the service worker is cache-first with no revalidation, so any device that had already visited Yatzy would keep serving the old, buggy `styles.css` forever without this bump, regardless of the fix being deployed. Verified via `npm run dev` + browser CDP viewport emulation (multiple widths, scoring-phase content, room-code title) that the new layout doesn't collide; `npm run check` (lint/format/build) passes with the same 13 pre-existing warnings.
- [x] 2026-09-17 — Post-migration cleanup: deleted the now-domainless old `coinchapp` Vercel project (`prj_hYpygjNbeeTmS2qnZzJEJVsY6hxw`), renamed `coinchapp-monorepo` back to `coinchapp` now that the name was free (`coinchapp.vercel.app` unaffected, verified 200 after rename). GitHub repo deletion and the `JQW` test-game cleanup are user-side manual steps (see Now above).
- [x] 2026-09-17 — Completed the `coinchapp` Vercel repoint: `coinchapp-monorepo` (Root Directory `apps/coinchapp`, all 4 env vars present) redeployed, verified live end-to-end in a real browser (created an online game — Supabase realtime works; searched a GIF — Giphy works), then `coinchapp.vercel.app` domain moved from the old project to `coinchapp-monorepo`. Confirmed the live production domain now serves the new deployment (`/`, `/coinche`, `/bouilla`, `/president`, `/bataillecorse` all 200). See `docs/DECISIONS.md`.
- [x] 2026-09-17 — Deduplicated `engine.js`: deleted the root `shared/js/engine.js` copy, `wordplayer.js` now imports the single `public/shared/js/engine.js` (a relative disk-path import into `public/` bundles fine with Vite — verified, not blocked by the publicDir-import restriction, which only applies to root-absolute URL imports). Added `shared/CONTRACT.md` documenting the public API of every shared module. Verified live via `npm run dev` + Pictionary (`wordplayer.html?game=pictionary`): rules text, word pull, and category color all work; `npm run check` passes, 13 pre-existing lint warnings unchanged. See `docs/DECISIONS.md`.
- [x] 2026-09-09 — Extracted `public/games/yatsy/render.js` (`window.YATZY_RENDER.createRenderer(deps)`, `render()` and its ~25 `build*`/`create*`/`render*` DOM helpers, ~685 lines) and `public/games/yatsy/session.js` (`window.YATZY_SESSION.createSessionController(deps)`, matchmaking wiring, session persistence, remote-state sync, ~600 lines) out of `app.js` (2204 → 1187 lines). Both factories close over `app.js`'s `state`/`elements` by reference and receive the rest of their dependencies (predicates, scoring helpers, the category-list and scoring-animation-flag getters/setters) through an explicit `deps` object, since `CATEGORIES` is rebuilt on every rule-settings change and the scoring-animation flag is shared with the local/robot scoring flow that stayed in `app.js`. `app.js` keeps single `const render = renderer.render` / `const session = ...` aliases so every existing call site (dozens of `render();` calls, `syncOnlineGameState()`, `leaveCurrentGame()`) kept working unchanged; the handful of `addEventListener` calls that referenced session handlers before their alias existed were wrapped in arrow functions to defer the lookup to click time. Verified live in the browser: solo + 2-player games, dice roll/lock, scoring, undo window, settings-panel rule toggle (which rebuilds `CATEGORIES` mid-session — the main risk this split carried), emoji reactions, and the online create/join flow failing gracefully with the expected "Connexion impossible" message (no Supabase configured locally). `npm run lint`/`format:check`/`build` all pass, lint warnings unchanged at 13. `app.js` still exceeds the 300-line limit (see Later) but now holds only the core game engine.
- [x] 2026-09-09 — Extracted `public/games/yatsy/storage.js` (`window.YATZY_STORAGE`): the raw `localStorage` read/write/remove primitives, moved out of `app.js` (2267 → 2204 lines). Key names and value shapes (rule settings, online session, boolean preferences) stay owned by `app.js`; the new file only knows JSON vs boolean vs raw string. Verified in the browser: solo game starts, dice roll, and a settings toggle all still work and persist to the right `localStorage` keys, zero console errors. First increment of the "Later" oversized-module split below.
- [x] 2026-09-09 — Deleted the ≈16.9 MB of dead weight the 2026-08-30 audit had flagged but left for manual review: `old1.jpg`, `old2.jpg`, `old3.jpg` (repo root), `public/assets/banner-games-hub-old.jpg`, `public/games/millionaire/questions_{en,es,fr}.json` (duplicate French content under EN/ES filenames, unused — the game loads `assets/data/*.json`), `public/games/easyfrog/style.css` (unused, Easy Frog is `kind: external`), and the two byte-identical, unused copies of `shared/js/dom.js` / `public/shared/js/dom.js`. Re-verified zero references repo-wide before deleting; `npm run lint`/`format:check`/`build` still pass.
- [x] 2026-09-04 — Yatzy GIF reactions via Giphy next to the emoji picker (local + online). Server-only `GIPHY_API_KEY`, 5s overlay, `pg-13`. See `docs/DECISIONS.md`.
- [x] 2026-09-01 — GameBoy Web hub tile uses `public/games/gameboy-web/assets/thumbnail.jpg` (original Game Boy photo). The SVG placeholder is gone.
- [x] 2026-09-01 — GameBoy Web tile launches `https://gameboy-web.vercel.app/binjgb/docs/simple.html` (`kind: external`, same tab). Removed the in-repo duplicate player. See `docs/DECISIONS.md`.
- [x] 2026-09-01 — GameBoy Web tile now launches the in-repo player (`kind: custom`, `/games/gameboy-web/index.html`) instead of GitHub. binjgb web player lives under `public/games/gameboy-web/vendor/` (eslint-ignored). Splash lists `porklike.gb` via `ROM/index.json` and can also open local `.gb`/`.gbc` files. Nintendo ROMs are not committed. See `docs/DECISIONS.md`.
- [x] 2026-08-30 — Added a 7d/30d/6m range switch and a daily trend chart to `/admin` (`306d19e`), modelled on the `nodali` project's `cms/analytics` page. Reimplemented rather than ported: nodali is Next.js with React, Tailwind and shadcn, this page is plain HTML/CSS/JS served verbatim from `public/` with no build. Scope was set by what `muchogames_events` stores (`game_id`, `created_at`), so six of nodali's eleven elements are not computable without new columns and much more personal data — put to the user, who chose the bounded scope and declined tabs. No charting library: one SVG polyline in `public/admin/trend-chart.js`, viewBox stretched by `preserveAspectRatio="none"` so it resizes with no listener. `totalLaunches` is now windowed instead of all-time, hence the caption naming the active period. Verified live on all three ranges plus an unknown range falling back to the default. See `docs/DECISIONS.md`.
- [x] 2026-08-30 — Fixed `/admin` loading neither its stylesheet nor its script (`f2fbb1a`). `public/admin/index.html` used relative `./admin.css` and `./admin.js`, but the page is served at `/admin` with no trailing slash, so both resolved to `/admin.css` and `/admin.js` at the root and 404'd. Because Vercel serves the 404 body as `text/plain`, the browser reported a MIME type refusal for the stylesheet rather than a missing file, which disguised the cause. The CSP `form-action 'none'` violation was a consequence: with `admin.js` absent nothing called `preventDefault`, so the login form attempted a native submit. Both paths are absolute now, matching the `/shared/css/base.css` and `/favicon.ico` references already in the file — which is precisely why those two worked. Verified live: every resource the page requests returns 200 with the correct MIME type.
- [x] 2026-08-30 — Moved `favicon.ico` from the repo root to `public/favicon.ico` (`ada97f1`), fixing a fourth 404 on `/admin` found while checking the page's resources. It only ever worked on the hub by accident: Vite resolved and content-hashed it for the root `index.html`, while `public/admin/index.html` is copied verbatim and its identical `/favicon.ico` reference resolved to nothing. Serving it from `publicDir` fixes both pages from one file and removes an asset from the repo root, which `AGENTS.md` forbids. The favicon is no longer content-hashed — acceptable, since browsers request `/favicon.ico` by convention whatever the HTML declares.
- [x] 2026-08-30 — Ran `0002_events.sql` on `multigames-db`, closing the last blocking configuration step. Verified live in the order that needed no write first: `/api/admin/stats` went from 500 to 200 `{"totalLaunches":0,"ranking":[]}`, proving the table exists and is readable by the service role, then `POST /api/track` answered 200 `{recorded:true}` and the row read back through stats. `/api/track` returns 200, not 204.
- [x] 2026-08-30 — Set `ADMIN_PASSWORD` on Vercel (Production, Sensitive) and verified the admin auth chain live: correct password → 200 + signed token, wrong password → 401, token with an altered HMAC → 401, valid token → passes the gate and fails only on the missing table. Rotating the demo value is still open above.
- [x] 2026-08-30 — Fixed `readJsonBody` (`e070ae9`): it guarded `JSON.parse` but not the `req.body` access above it, and on Vercel's Node runtime `req.body` is a getter that parses eagerly and throws `Invalid JSON`. Every POST with a malformed body therefore answered 500 and logged an `unhandled` error — including cheap unauthenticated 500s on `/api/track` — instead of letting the handler validate its input. Found by reading the runtime logs while debugging the admin login, which is the priority-3 observability work paying for itself. Verified live: garbage and empty bodies now give 401 on login and 400 `invalid-event` on `/api/track`.
- [x] 2026-08-30 — Priority 5, code part: cleaned up the dead design tokens in `public/shared/css/base.css`. The audit's "change `::root` to `:root`" was measured in the browser first and turned out to be harmful — it would have painted light grey text on the beige background of the hub and `wordplayer.html`. Kept `--font-sans` only (which fixes `body` falling back to Times New Roman) and dropped the 13 other tokens, none of which had a live consumer. See `docs/DECISIONS.md`. The deletion part is still open above, at the user's request.
- [x] 2026-08-30 — Priority 4 of the audit action plan: CI is a real gate for the first time, `npm run check` exits 0. Repointed the ESLint globs at `public/**` and the repo root (1207 errors → 0 errors, 16 warnings; 1164 were phantom `no-undef` from a glob aimed at the non-existent `apps/**`), added `.prettierignore` and `endOfLine: "auto"`, formatted the 52 files of the maintained surface while excluding `public/games/**` behind a documented ratchet, and switched CI to `npm ci`. Fixed 4 real defects found along the way in `millionaire/app.js` (2 duplicate keys, 8 useless escapes, 1 narrow no-break space). Scope chosen by the user over a full 101-file sweep — see `docs/DECISIONS.md`.
- [x] 2026-08-30 — Priority 3 of the audit action plan (minimum scope, as asked): `sendError` now logs 5xx detail as one-line JSON and returns a generic message, all 8 serverless handlers are wrapped in `withErrorHandling`, and a malformed body was meant to yield a 400 instead of a 500 — that last part only actually worked from `e070ae9` onwards, see above. Client-side error reporting deliberately skipped — see `docs/DECISIONS.md`.
- [x] 2026-08-30 — Priority 2 of the audit action plan: added `vercel.json` security headers (strict CSP on `/admin`, `SAMEORIGIN` framing, `no-store` on `/api/*`), split admin auth into `POST /api/admin/login` issuing a 1h HMAC-signed token verified with `timingSafeEqual`, throttled login and `/api/track` in memory, and documented `ADMIN_PASSWORD` in `.env.example`.
- [x] 2026-08-30 — Priority 1 of the audit action plan: closed the destructive Yatzy API surface. Removed the unauthenticated bulk-purge endpoint, required a seat token on `DELETE` and on seat reclaim via `join`, bounded `gameState` writes to 64 KB, and escaped the winner banner render.
- [x] 2026-08-30 — Full technical audit (security, UI/UX, architecture, maintainability). Score 4.75/10, verdict "production-ready under conditions"; the 5 priorities are tracked under Next.
- [x] 2026-08-15 — Bootstrapped agent context architecture (`AGENTS.md` + `docs/`), migrated legacy specs, removed old sync-flow scripts.
- [x] 2026-08-30 — Added a password-protected admin stats page (`/admin`) showing the game launch ranking, backed by the new `muchogames_events` Supabase table, `api/track.js`, and `api/admin/stats.js`.
- [x] 2026-08-16 — Migrated Yatzy multiplayer from Firebase Realtime Database to Supabase Postgres (shared `multigames-db` project) via new `api/yatsy/games/*` Vercel functions; moved hub hosting from Firebase Hosting to Vercel (auto-deploy via Git integration, superseding the old "wire Firebase Hosting into CI" item). Verified end-to-end in production.
